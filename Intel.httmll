<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Opeyemi, Will You Be My Valentine? 💖</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      font-family: 'Comic Sans MS', 'Segoe UI', sans-serif;
    }
    .card {
      background: white;
      padding: 40px;
      border-radius: 24px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
      text-align: center;
      max-width: 380px;
      width: 90%;
    }
    h1 {
      color: #ff4d6d;
      margin-bottom: 15px;
    }
    p {
      font-size: 18px;
      margin-bottom: 25px;
    }
    .buttons {
      display: flex;
      justify-content: center;
      gap: 20px;
      position: relative;
    }
    button {
      padding: 12px 26px;
      font-size: 16px;
      border: none;
      border-radius: 999px;
      cursor: pointer;
      transition: transform 0.2s ease;
    }
    #yes {
      background: #ff4d6d;
      color: white;
    }
    #no {
      background: #ddd;
      color: #333;
      position: absolute;
    }
    .heart {
      font-size: 46px;
      animation: pulse 1s infinite;
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body>
  <div class="card" id="card">
    <div class="heart">❤️</div>
    <h1>Opeyemi… will you be my Valentine?</h1>
    <p>Say yes and I’ll spoil you endlessly 😌</p>
    <div class="buttons">
      <button id="yes">Yes 💕</button>
      <button id="no">No 💔</button>
    </div>
  </div>  <script>
    const noBtn = document.getElementById('no');
    const yesBtn = document.getElementById('yes');
    const card = document.getElementById('card');

    let noCount = 0;
    const messages = [
      "Are you sure? 🥺",
      "Like… really sure? 😢",
      "Opeyemi pls think about it 😭",
      "This will break my heart 💔",
      "Last chance 😔"
    ];

    noBtn.addEventListener('mouseover', () => {
      const x = Math.random() * 220 - 110;
      const y = Math.random() * 220 - 110;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    });

    noBtn.addEventListener('click', () => {
      noCount++;
      card.querySelector('p').innerText = messages[Math.min(noCount - 1, messages.length - 1)];
    });

    yesBtn.addEventListener('click', () => {
      card.innerHTML = `
        <div class="heart">💘</div>
        <h1>YAYYY OPEYEMI!!!</h1>
        <p>You’re officially my Valentine 🥹❤️<br/>I can’t wait to spoil you.</p>
      `;
    });
  </script></body>
</html>
